applicationType: default
defaultPackage: hello.world
testFramework: spock
sourceLanguage: groovy
buildTool: gradle
features: [
  annotation-api, app-name, gradle, groovy, groovy-application, 
  h2, hibernate-gorm, hibernate-validator,
  http-client, jackson-databind, logback, micronaut-build, 
  netty-server, readme, shade, spock, yaml]
